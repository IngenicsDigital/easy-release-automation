[project]
name = "easy_release_automation"
version = "0.4.0.dev0"
authors = []

description = "..."
requires-python = ">=3.9,<3.12"

dependencies = ["{ MAIN-DEPENDENCY }", "pip==24.0", "pydantic==2.7.1"]

[project.optional-dependencies]
test = ["{ OPTIONAL-TEST-DEPENDENCY }", "black == 24.4.2"]
